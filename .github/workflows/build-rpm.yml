name: Build RPM
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Install rpmbuild
      run: sudo apt-get install rpm

    - name: Run build
      run:
        |
        cd rpm-builder/
        sudo php pack.php