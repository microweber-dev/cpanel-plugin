name: Build RPM
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Run build
      run:
        |
        cd rpm-builder/
        sudo php pack.php